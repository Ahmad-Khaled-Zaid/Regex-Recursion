let str = " dogs kep fight w4ith cats";
//  regular expression is a way to search through a string of text
//  Why do we use it ?
// 1- Validate strings against certain rules
// 2- Find substring within a string
// 3- Replace part of the string
// let regex = /s/g; check the letter s inside the string
// let regex = /[0,9]/g; // check if there is any number inside the string
// let regex = /\d/g; // check if there is any number inside the string (short syntax)
// let regex = /[A-Z]/g;  //check if the string contain any letters g mean sensitive , gi means insensitive
// when we use global flag it allows to match any where in a string 
// if we turn the global off, it will match the first result it found

// let regex = /e+/g match any e in a row, but match at least one e
// /\t/g will match any word character
// /\s/g will match any white space 
// /\S/g is going to match any thing not white space
// /\W/g match any thing not character
// /\w{4}/g git any 4 digits in row
// /\w{4,}/g git any 4 digits in row or more 
// /\[fc]at/g will get any words start with f or c and ends with at

// console.log(regex.test(str));
let x;
console.log(x);
